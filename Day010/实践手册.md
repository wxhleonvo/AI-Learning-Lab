# Day010 实践手册：错误处理与重试

> 路线位置：180 天路线 → Phase 1 → Day010
> 前置：Day001-Day009（IChatClient / ChatOptions / Structured Output / Prompt 模板化）
> 今日主题：**把「假设调用永远成功」的 LLM 调用链路，加上一层错误处理与重试保护——让它在网络抖动、429 限流、Key 过期、输出截断时优雅退化，而不是崩溃**

---

## 1. 今日目标

完成后，你必须能够：

1. 识别 LLM 调用的**典型失败模式**及其 HTTP 状态码 → 异常类型映射。
2. 说出**哪些错误可重试、哪些绝对不能重试**（幂等性）。
3. 手写 **指数退避 + 抖动（Exponential Backoff with Jitter）** 重试循环。
4. 用 `CancellationTokenSource` 实现**整体超时控制**，避免单次调用无限阻塞。
5. 处理**结构化输出解析失败**（TryGetResult=false / FinishReason=length）的降级策略。
6. 引入 `Microsoft.Extensions.Http.Resilience`（Polly 8 封装），对比手写 vs 工业级重试。
7. 复用 Day009 的模板渲染链路，把 `GetResponseAsync<T>` 包进 `InvokeWithRetryAsync` 保护层。
8. 建立「**重试不是免费的**」认知——每次重试都烧 Token，要有最大次数 + 熔断意识。

---

# 2. 今天只学什么

| 概念 | 今日要求 |
|---|---|
| LLM 调用失败分类 | 429/500/503 可重试；400/401/402 不可重试 |
| Exception 类型识别 | HttpRequestException / ClientResultException / TaskCanceledException |
| 指数退避 | delay = base * 2^attempt |
| 抖动（Jitter） | + random(0, delay)，避免惊群 |
| 最大重试次数 | 通常 3-5 次 |
| CancellationTokenSource | 整体超时 + 每次重试独立超时 |
| 幂等性 | 重试必须幂等；POST 非幂等要谨慎 |
| 结构化输出降级 | TryGetResult=false → 取 RawText / 打日志 / 抛业务异常 |
| FinishReason 分类 | Stop（正常）/ Length（截断）/ ContentFilter（安全拦截）/ Error（API 错误） |
| Microsoft.Extensions.Http.Resilience | Polly 8 官方封装，工业级重试策略 |

今天**不要**学习：

- Circuit Breaker（熔断器）——Phase 5 Day117
- Fallback（降级策略）——Phase 5 Day118
- Rate Limiter（限流）——Phase 5 Day115
- 分布式追踪 / 可观测性——Phase 5 Day110

---

# 3. 官方资料

## 3.1 主资料：.NET AI 概念文档（扫读）

https://learn.microsoft.com/zh-cn/dotnet/ai/concepts/

重点搜索 "error" / "retry" / "exception"——官方有对 LLM 调用异常的分类说明。虽然 Microsoft.Extensions.AI 不内置重试，但文档推荐用 `Microsoft.Extensions.Http.Resilience` 处理底层 HTTP 故障。

## 3.2 参考：HTTP 重试策略（Polly / Microsoft.Extensions.Http.Resilience）

https://learn.microsoft.com/zh-cn/dotnet/core/resilience/http-resilience

今天只看：

1. `AddResilienceHandler` 注册重试策略
2. `RetryStrategyOptions` 的核心配置：MaxRetryAttempts、ShouldHandle、DelayGenerator（指数退避）

**看完必须获得的知识**：工业级重试不是自己写 for 循环，而是用官方 Resilience 库（Polly 8 封装），声明式配置 `ShouldHandle`（哪些异常重试）和 `DelayGenerator`（退避曲线）。

## 3.3 LLM API 错误码速查

```text
HTTP 400 Bad Request         → Prompt 超长 / 参数非法 / 格式错误 → 不可重试
HTTP 401 Unauthorized         → Key 过期 / 无效 → 不可重试（除非 Key 轮换）
HTTP 402 Payment Required     → 余额不足 → 不可重试（除非充值）
HTTP 429 Too Many Requests    → 限流 → 可重试（Retry-After 头告知等待时间）
HTTP 500 Internal Server Error → 服务端错误 → 可重试
HTTP 502 Bad Gateway          → 代理错误 → 可重试
HTTP 503 Service Unavailable  → 服务不可用 → 可重试（Retry-After 头）
TaskCanceledException         → 超时 → 谨慎重试（可能请求已到达服务端）
```

---

# 4. 创建 Day010 项目

## 目录与依赖

```powershell
cd G:\AI学习\AI-Learning-Lab
dotnet new console -n RetryDemo -o Day010\RetryDemo
cd Day010\RetryDemo

dotnet add package Microsoft.Extensions.AI --version 10.9.0
dotnet add package Microsoft.Extensions.AI.OpenAI --version 10.9.0
dotnet add package Microsoft.Extensions.Configuration --version 10.0.11
dotnet add package Microsoft.Extensions.Configuration.Json --version 10.0.11
dotnet add package Microsoft.Extensions.Configuration.UserSecrets --version 10.0.11
dotnet add package Microsoft.SemanticKernel --version 1.80.1
dotnet add package Microsoft.Extensions.Http.Resilience --version 10.0.11
dotnet add package Polly --version 8.5.0
```

> **关于包选择**：`Microsoft.Extensions.Http.Resilience` 是 .NET 官方的 HTTP 韧性封装（内部用 Polly 8），但 `ResiliencePipelineBuilder` / `RetryStrategyOptions` 等核心类型在 **`Polly` 命名空间**，需要**额外引 `Polly` 包**。两者配合使用：M.E.Http.Resilience 提供 HTTP 专用扩展，Polly 提供通用 Pipeline。

## User Secrets 与配置

```powershell
dotnet user-secrets init
dotnet user-secrets set DashScopeKey "<你的-Key>"
dotnet user-secrets set DashScopeModel "qwen3.7-flash"
dotnet user-secrets list
```

> **模型选择建议**：Day010 重点是错误分类 + 重试策略，需要稳定返回 JSON 结构化输出。推荐 **qwen3.7-flash**（非推理模型，结构化输出稳，性价比高）。
> 推理模型（glm-5.2 等）的 reasoning tokens 会挤占 MaxOutputTokens，干扰错误处理实验的纯净度（FinishReason=length + RawText 为空，你 Day009 刚踩过），今天**不要用**。
> 如果你的可用模型列表里没有 qwen3.7-flash，qwen3.8-flash / qwen3.8-max-0902 / kimi-k3 也可以。

## csproj

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <UserSecretsId>dotnet-RetryDemo-20260908</UserSecretsId>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.AI" Version="10.9.0" />
    <PackageReference Include="Microsoft.Extensions.AI.OpenAI" Version="10.9.0" />
    <PackageReference Include="Microsoft.Extensions.Configuration" Version="10.0.11" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Json" Version="10.0.11" />
    <PackageReference Include="Microsoft.Extensions.Configuration.UserSecrets" Version="10.0.11" />
    <PackageReference Include="Microsoft.SemanticKernel" Version="1.80.1" />
    <PackageReference Include="Microsoft.Extensions.Http.Resilience" Version="10.0.11" />
    <PackageReference Include="Polly" Version="8.5.0" />
  </ItemGroup>
  <ItemGroup>
    <None Update="appsettings.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
    <None Update="examples.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
    <None Update="Prompts\**\*">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>
</Project>
```

## 目录结构

```text
Day010/
├── 实践手册.md
├── notes.md
├── troubleshooting.md
├── resources.md
├── screenshots/
└── RetryDemo/
    ├── RetryDemo.csproj
    ├── Program.cs
    ├── appsettings.json
    ├── examples.json              ← 复用 Day009
    └── Prompts/
        └── review-analysis.txt   ← 复用 Day009
```

> Prompts / examples.json 直接从 Day009 复制过来（Day009 模板 + 示例今天继续用）。

---

# 5. 实验一：识别错误类型（故意制造失败）

先建立「**失败分类 + 是否可重试**」的认知。用错误 Key 故意触发 401，用超长 Prompt 故意触发 400。

```csharp
using System.ComponentModel;
using System.Net;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Text.RegularExpressions;
using Microsoft.Extensions.AI;
using Microsoft.Extensions.Configuration;
using Microsoft.SemanticKernel;
using OpenAI;
using System.ClientModel;

Encoding.RegisterProvider(CodePagesEncodingProvider.Instance);
Console.InputEncoding = Encoding.GetEncoding(936);

IConfigurationRoot config = new ConfigurationBuilder()
    .AddJsonFile("appsettings.json", optional: false)
    .AddUserSecrets<Program>()
    .Build();

string? model = config["DashScopeModel"];
string? realKey = config["DashScopeKey"];

OpenAIClientOptions clientOptions = new()
{
    Endpoint = new Uri("https://dashscope.aliyuncs.com/compatible-mode/v1")
};

IChatClient client = new OpenAIClient(new ApiKeyCredential(realKey!), clientOptions)
    .GetChatClient(model).AsIChatClient();

// ---------- 实验一：错误类型识别 ----------
Console.WriteLine("===== 实验一：错误类型识别 =====");

// 1. 错误 Key → 401 Unauthorized
Console.WriteLine("\n1. 错误 Key → 预期 401：");
IChatClient badClient = new OpenAIClient(new ApiKeyCredential("sk-invalid-key"), clientOptions)
    .GetChatClient(model).AsIChatClient();
try
{
    await badClient.GetResponseAsync("你好", new ChatOptions { Temperature = 0.1f });
    Console.WriteLine("  意外：没报错");
}
catch (Exception ex)
{
    ClassifyException(ex);
}

// 2. 超长 Prompt → 400 Bad Request
Console.WriteLine("\n2. 超长 Prompt → 预期 400 或 context length exceeded：");
string hugePrompt = new string('x', 100_000);
try
{
    await client.GetResponseAsync(hugePrompt, new ChatOptions { Temperature = 0.1f });
    Console.WriteLine("  意外：没报错");
}
catch (Exception ex)
{
    ClassifyException(ex);
}

// ---------- 异常分类 ----------
static void ClassifyException(Exception ex)
{
    string category = ex switch
    {
        HttpRequestException hre => $"HTTP {hre.StatusCode}（HttpRequestException）",
        ClientResultException cre  => $"API 错误（ClientResultException）",
        TaskCanceledException      => "超时（TaskCanceledException）",
        JsonException              => "JSON 解析失败（JsonException）",
        InvalidOperationException  => "业务异常（InvalidOperationException）",
        _                          => $"其他（{ex.GetType().Name}）"
    };

    bool retryable = ex switch
    {
        HttpRequestException hre => hre.StatusCode is HttpStatusCode.TooManyRequests
                                       or HttpStatusCode.InternalServerError
                                       or HttpStatusCode.BadGateway
                                       or HttpStatusCode.ServiceUnavailable,
        TaskCanceledException    => true,   // 网络超时可重试；业务超时不可
        ClientResultException    => false,  // 通常是 400/401/402
        _                        => false
    };

    Console.WriteLine($"  类型：{category}");
    Console.WriteLine($"  可重试：{(retryable ? "是 ✅" : "否 ❌")}");
    Console.WriteLine($"  消息：{ex.Message[..Math.Min(120, ex.Message.Length)]}");
}
```

运行，记录每种错误的实际异常类型 + 可重试性。这是今天所有重试逻辑的决策依据。

### 必须得出的结论

```text
可重试 ✅：429 / 500 / 503 / 网络超时
不可重试 ❌：400 / 401 / 402 / 业务异常（模板变量缺失等）
谨慎重试 ⚠️：401 在 Key 轮换场景可能可重试；TaskCanceledException 可能请求已到达服务端
```

---

# 6. 实验二：指数退避 + 抖动（手写实现）

```csharp
// ---------- 实验二：手写重试 ----------
Console.WriteLine("\n===== 实验二：手写指数退避 + 抖动 =====");

// 正常调用（验证路径走通）
ChatResponse<ReviewAnalysis> result = await InvokeWithRetryAsync(
    client,
    "你好，请用 JSON 返回 {\"sentiment\":\"Positive\",\"score\":9}",
    new ChatOptions { Temperature = 0.1f });

if (result.TryGetResult(out ReviewAnalysis? r))
    Console.WriteLine($"  成功：情感={r.Sentiment}, 评分={r.Score}");

// ---------- 手写重试实现 ----------
static async Task<ChatResponse<ReviewAnalysis>> InvokeWithRetryAsync(
    IChatClient chatClient,
    string prompt,
    ChatOptions options,
    int maxRetries = 3,
    CancellationToken ct = default)
{
    int attempt = 0;
    Exception? lastException = null;

    while (attempt <= maxRetries)
    {
        try
        {
            attempt++;
            Console.WriteLine($"  [重试 {attempt}/{maxRetries + 1}] 调用中...");

            var response = await chatClient.GetResponseAsync<ReviewAnalysis>(
                prompt, options, cancellationToken: ct);

            // 结构化输出解析失败：降级处理
            if (!response.TryGetResult(out _))
            {
                // 不是网络错误，是模型输出问题 → 不重试，直接抛业务异常
                throw new InvalidOperationException(
                    $"结构化输出解析失败，FinishReason={response.FinishReason}，" +
                    $"原始：{response.Text?.Substring(0, Math.Min(100, response.Text?.Length ?? 0))}");
            }

            Console.WriteLine($"  [尝试 {attempt}] ✅ 成功");
            return response;
        }
        catch (Exception ex) when (IsRetryable(ex))
        {
            lastException = ex;
            if (attempt > maxRetries) break;

            // 指数退避 + 抖动
            // base=1s, attempt=1→1s, 2→2s, 3→4s, 4→8s
            double delayMs = 1000 * Math.Pow(2, attempt - 1);
            // + random jitter [0, delay] 避免惊群
            double jitterMs = new Random().NextDouble() * delayMs;
            double totalMs = delayMs + jitterMs;

            Console.WriteLine($"  [尝试 {attempt}] ⏳ 可重试错误：{ex.GetType().Name}，" +
                              $"等待 {totalMs/1000:F1}s 后重试");
            await Task.Delay((int)totalMs, ct);
        }
        catch (TaskCanceledException)
        {
            Console.WriteLine($"  [尝试 {attempt}] ❌ 超时，不再重试");
            throw;  // 业务超时直接抛，不重试
        }
        catch
        {
            Console.WriteLine($"  [尝试 {attempt}] ❌ 不可重试错误");
            throw;  // 不可重试的错误直接抛
        }
    }

    throw new InvalidOperationException(
        $"重试 {maxRetries + 1} 次后仍失败", lastException);
}

static bool IsRetryable(Exception ex) => ex switch
{
    HttpRequestException hre => hre.StatusCode is HttpStatusCode.TooManyRequests
                                   or HttpStatusCode.InternalServerError
                                   or HttpStatusCode.BadGateway
                                   or HttpStatusCode.ServiceUnavailable,
    ClientResultException    => false,
    TaskCanceledException   => false,  // 超时由 CancellationToken 控制，不在重试循环里处理
    _                        => false
};
```

### 预期输出

```text
===== 实验二：手写指数退避 + 抖动 =====
  [重试 1/4] 调用中...
  [尝试 1] ✅ 成功
  成功：情感=Positive, 评分=9
```

### 关键设计点

1. **`when (IsRetryable(ex))`**：只有可重试异常才进入退避逻辑，其他直接抛出。
2. **指数退避**：`1000 * 2^(attempt-1)`，1s → 2s → 4s → 8s。
3. **抖动**：`+ random(0, delay)`，避免多个客户端同时重试。
4. **TaskCanceledException 单独处理**：业务超时（CancellationToken 触发）不重试。
5. **结构化输出解析失败不重试**：模型输出问题，重试也一样。

---

# 7. 实验三：用错误 Key 触发重试循环（验证可重试逻辑）

用正确 Key 跑不出重试，现在制造 429 或 500 来验证退避行为。最简单的办法：**让同一个错误 Key 快速触发限流**，或者**模拟 Polly 风格的断路器禁用**。

但今天不用真的触发限流（会烧额度），而是**把 `IsRetryable` 临时放宽**，让 401 也进入重试循环，观察退避延迟：

```csharp
// 临时：让 401 也可重试（仅用于演示退避行为）
static bool IsRetryableDemo(Exception ex) => ex switch
{
    HttpRequestException => true,     // 演示：所有 HTTP 错误都重试
    _                    => false
};

Console.WriteLine("\n===== 实验三：退避行为演示（故意让 401 也重试）====");
Console.WriteLine("（临时放宽 IsRetryable，让错误 Key 触发退避循环）");

try
{
    await InvokeWithRetryDemoAsync(badClient, "你好", new ChatOptions { Temperature = 0.1f });
}
catch (InvalidOperationException ex)
{
    Console.WriteLine($"  最终：{ex.Message}");
}
```

预期输出（退避时间逐次翻倍 + 抖动）：

```text
===== 实验三：退避行为演示 =====
  [重试 1/4] 调用中...
  [尝试 1] ⏳ 可重试错误：HttpRequestException，等待 1.2s 后重试
  [重试 2/4] 调用中...
  [尝试 2] ⏳ 可重试错误：HttpRequestException，等待 2.1s 后重试
  [重试 3/4] 调用中...
  [尝试 3] ⏳ 可重试错误：HttpRequestException，等待 4.3s 后重试
  [重试 4/4] 调用中...
  最终：重试 4 次后仍失败
```

### 必须得出的结论

```text
指数退避：1s → 2s → 4s → 8s
抖动：每次 delay 随机 ±[0, delay] 倍
重试次数：通常 3-5 次（太多会烧 Token）
总等待时间：base * (2^n - 1) + jitter ≈ 15-31s（3 次重试）
```

---

# 8. 实验四：整体超时 + 取消（CancellationTokenSource）

重试循环必须受**整体超时**约束。比如设 30 秒硬限制，无论重试到第几次，30 秒到了必须停。

```csharp
// ---------- 实验四：整体超时 ----------
Console.WriteLine("\n===== 实验四：整体超时控制 =====");

using var cts = new CancellationTokenSource(TimeSpan.FromSeconds(5));  // 5 秒硬限制

try
{
    // 用 badClient + 放宽 IsRetryable，触发多次退避但 5 秒后被取消
    await InvokeWithRetryAsync(badClient, "你好",
        new ChatOptions { Temperature = 0.1f },
        maxRetries: 10,  // 故意多设，实际会被 5s 超时截断
        ct: cts.Token);
}
catch (OperationCanceledException)
{
    Console.WriteLine("  ⏱️ 5 秒整体超时，正确取消");
}
```

### 退避循环里的关键改动

```csharp
// Task.Delay 也要传 ct，被整体超时时立即取消等待
await Task.Delay((int)totalMs, ct);

// GetResponseAsync 也要传 ct
var response = await chatClient.GetResponseAsync<ReviewAnalysis>(prompt, options, cancellationToken: ct);
```

### 必须得出的结论

```text
两层超时：
  ① 整体超时（CancellationTokenSource）：业务可接受的最大等待时间
  ② 单次调用超时：由 OpenAIClientOptions.Timeout 控制
超时不等于可重试：业务超时 → 不重试；网络超时 → 谨慎重试（可能请求已到达服务端）
```

---

# 9. 实验五：结构化输出降级（FinishReason 分类处理）

Day009 你见过：`FinishReason=length` + RawText 为空。这不是网络错误，重试没用。今天把它纳入异常链路，**降级为 RawText 回退**（结构化解析失败时返回原始文本，允许上层决定要不要展示给用户）。

```csharp
// ---------- 实验五：FinishReason 分类处理 ----------
static async Task<(ReviewAnalysis? Structured, string? RawText, ChatFinishReason? Reason)>
    InvokeWithGracefulDegradationAsync(
    IChatClient chatClient,
    string prompt,
    ChatOptions options,
    CancellationToken ct = default)
{
    try
    {
        var response = await chatClient.GetResponseAsync<ReviewAnalysis>(prompt, options, cancellationToken: ct);

        if (response.TryGetResult(out ReviewAnalysis? r))
        {
            return (r, response.Text, response.FinishReason);
        }

        // 结构化解析失败，但有原始文本 → 降级
        if (!string.IsNullOrWhiteSpace(response.Text))
        {
            Console.WriteLine($"  [降级] 结构化解析失败，FinishReason={response.FinishReason}，" +
                              $"返回原始文本（{response.Text.Length} 字符）");
            return (null, response.Text, response.FinishReason);
        }

        // RawText 也为空 → 严重问题
        return (null, null, response.FinishReason);
    }
    catch (Exception ex) when (!IsRetryable(ex))
    {
        Console.WriteLine($"  [异常] {ex.GetType().Name}: {ex.Message[..Math.Min(100, ex.Message.Length)]}");
        return (null, null, null);
    }
}
```

### 调用示例

```csharp
var (structured, raw, reason) = await InvokeWithGracefulDegradationAsync(
    client, prompt, new ChatOptions { Temperature = 0.1f, MaxOutputTokens = 4000 });

if (structured is not null)
    Console.WriteLine($"✅ 结构化成功：{structured.Sentiment} / {structured.Score}");
else if (raw is not null)
    Console.WriteLine($"⚠️ 降级到 RawText（{reason}）：{raw[..Math.Min(80, raw.Length)]}…");
else
    Console.WriteLine($"❌ 完全失败（{reason}）");
```

### FinishReason 完整分类

| FinishReason | 含义 | 处理策略 |
|---|---|---|
| `Stop` | 正常结束 | 结构化解析 / 用 RawText |
| `Length` | 输出截断（Token 预算不够） | 降级 RawText / 增加预算 / 截断后尝试 JSON 修复 |
| `ContentFilter` | 安全过滤拦截 | 告知用户 / 记录日志 |
| `Error` | API 端错误 | 进入重试循环 |
| `ToolCalls` | 模型请求调用工具（Phase 4） | 不在今天处理 |

---

# 10. 实验六：Microsoft.Extensions.Http.Resilience（Polly 8）

手写重试够用，但生产项目用官方 Resilience 库（Polly 8 封装）。优势：声明式配置、可组合、已内置指数退避 + 抖动。

## 创建 ResiliencePipeline

```csharp
using System.Net;
using Polly;

// ---------- 实验六：Resilience Pipeline ----------
Console.WriteLine("\n===== 实验六：Polly 8 Resilience Pipeline =====");

// Polly 8 不直接包 IChatClient，而是包整个代码块
var pipeline = new ResiliencePipelineBuilder()
    .AddRetry(new RetryStrategyOptions
    {
        MaxRetryAttempts = 3,
        ShouldHandle = new PredicateBuilder<Exception>()
            .Handle<HttpRequestException>(e => e.StatusCode is HttpStatusCode.TooManyRequests
                                                    or HttpStatusCode.InternalServerError
                                                    or HttpStatusCode.ServiceUnavailable)
            .Handle<TaskCanceledException>(),
        DelayGenerator = args =>
        {
            double delayMs = 1000 * Math.Pow(2, args.AttemptNumber);
            double jitterMs = new Random().NextDouble() * delayMs;
            return new ValueTask<TimeSpan?>(TimeSpan.FromMilliseconds(delayMs + jitterMs));
        },
        OnRetry = args =>
        {
            Console.WriteLine($"  [Polly] 第 {args.AttemptNumber + 1} 次重试，" +
                              $"等待 {args.RetryDelay.TotalSeconds:F1}s，原因：{args.Outcome.Exception?.GetType().Name}");
            return default;
        }
    })
    .Build();
```

## 在 RetryStrategyOptions 里调用

```csharp
// Polly 8 不直接包 IChatClient，而是包整个代码块
var pollyResult = await pipeline.ExecuteAsync(async ct =>
{
    Console.WriteLine("  [Polly] 执行调用...");
    return await client.GetResponseAsync<ReviewAnalysis>(
        "你好，请用 JSON 返回 sentiment/score/keywords/conclusion",
        new ChatOptions { Temperature = 0.1f, MaxOutputTokens = 1000 },
        cancellationToken: ct);
}, CancellationToken.None);

if (pollyResult.TryGetResult(out ReviewAnalysis? pr))
    Console.WriteLine($"  [Polly] ✅ 成功：情感={pr.Sentiment}");
```

### 必须得出的结论

```text
手写重试：for 循环 + IsRetryable + 指数退避
Resilience Pipeline：声明式配置 + 可组合 + 内置 Polly
生产项目选后者（扩展性：后续加 Circuit Breaker / Fallback 无缝衔接）
学习阶段选前者（理解退避原理）
```

---

# 11. 实验七：Day009 模板链路 + 重试保护层（整合）

把 Day009 的模板渲染链路和今天的重试保护层整合，形成完整的「模板 → 渲染 → 带重试调用 → 优雅降级」生产级链路。

复用 Day009 的资源（Prompts/*.txt、examples.json），直接 `File.Copy` 过来：

```csharp
// ---------- Day009 模板链路（简化版）----------
static string RenderSimple(string template, Dictionary<string, string?> variables)
{
    ValidateVariables(template, variables);
    return Regex.Replace(template, @"\{\{\$?(\w+)\}\}", match =>
    {
        string name = match.Groups[1].Value;
        return variables.TryGetValue(name, out string? v) && v is not null
            ? v
            : throw new InvalidOperationException($"缺少变量 {name}");
    });
}

static void ValidateVariables(string template, Dictionary<string, string?> variables)
{
    var required = Regex.Matches(template, @"\{\{\$?(\w+)\}\}")
        .Select(m => m.Groups[1].Value).ToHashSet();
    var missing = required.Where(n => !variables.ContainsKey(n)).ToList();
    if (missing.Count > 0)
        throw new InvalidOperationException($"模板缺少变量 {string.Join(", ", missing)}");
}
```

### 整合后的主循环

```csharp
// ---------- 整合：模板渲染 + 重试 + 降级 ----------
string template = await File.ReadAllTextAsync("Prompts/review-analysis.txt");

var jsonOpts = new JsonSerializerOptions(JsonSerializerDefaults.Web);
jsonOpts.Converters.Add(new JsonStringEnumConverter());
List<FewShotExample> examples = JsonSerializer.Deserialize<List<FewShotExample>>(
    await File.ReadAllTextAsync("examples.json"), jsonOpts)!;

string role = config["Prompt:Role"] ?? "专业的客户体验分析师";
string input = "这个产品续航不行，电池衰减太快了，失望。";

string BuildExamplesSection(List<FewShotExample> exs)
{
    if (exs.Count == 0) return "";
    var sb = new StringBuilder();
    foreach (var ex in exs)
    {
        sb.AppendLine($"评论：{ex.Review}");
        sb.AppendLine($"输出：{JsonSerializer.Serialize(ex.Output, jsonOpts)}");
    }
    return sb.ToString();
}

var vars = new Dictionary<string, string?>
{
    ["role"] = role,
    ["examples"] = BuildExamplesSection(examples.Take(3).ToList()),
    ["input"] = input,
};

string prompt = RenderSimple(template, vars);

using var cts = new CancellationTokenSource(TimeSpan.FromSeconds(30));

// 关键：两层保护
var (structured, raw, reason) = await InvokeWithGracefulDegradationAsync(
    client, prompt,
    new ChatOptions { Temperature = 0.1f, MaxOutputTokens = 4000 },
    cts.Token);

if (structured is not null)
{
    Console.WriteLine($"✅ 成功：情感={structured.Sentiment} / 评分={structured.Score} " +
                      $"/ 关键词={string.Join("、", structured.Keywords ?? [])}");
}
else if (raw is not null)
{
    Console.WriteLine($"⚠️ 降级：FinishReason={reason}");
    Console.WriteLine($"   RawText：{raw[..Math.Min(150, raw.Length)]}...");
}
else
{
    Console.WriteLine($"❌ 完全失败：reason={reason}");
}
```

### 必须得出的结论

```text
完整的生产级 LLM 调用链路（Day007-Day010）：
  ① 模板渲染 + ValidateVariables fail-fast     ← Day009
  ② 整体超时 CancellationTokenSource            ← Day010
  ③ 指数退避 + 抖动重试（仅可重试异常）          ← Day010
  ④ 结构化输出解析失败 → RawText 降级            ← Day006 + Day010
  ⑤ 不可重试异常 → 直接抛出                      ← Day010
```

---

# 12. 今天不要做的事情

- 不要引入 Circuit Breaker / Fallback / Rate Limiter——Phase 5
- 不要用 Handlebars 模板引擎
- 不要把 Polly 挂在 HttpClient 上做全局重试——今天是代码块级重试
- 不要把所有异常都重试（400/401/402 绝对不能重试）
- 不要无限制重试（5 次足够，太多烧 Token）
- 不要忘记整体超时（重试循环必须受 CancellationTokenSource 约束）

---

# 13. 今天必须完成的实验

| 实验 | 内容 | 要求 |
|---|---|---|
| 实验一 | 错误 Key → 401 / 超长 Prompt → 400，记录异常类型与可重试性 | 必须 |
| 实验二 | 手写指数退避 + 抖动重试循环 | 必须 |
| 实验三 | 放宽 IsRetryable 演示退避延迟（1s→2s→4s→8s） | 必须 |
| 实验四 | CancellationTokenSource 整体超时控制 | 必须 |
| 实验五 | FinishReason 分类 + RawText 降级 | 必须 |
| 实验六 | Microsoft.Extensions.Http.Resilience Pipeline | 必须 |
| 实验七 | Day009 模板链路 + 重试保护层整合 | 必须 |

---

# 14. notes.md 必须回答的问题

1. LLM 调用的 4 种常见异常类型是什么？各自对应什么 HTTP 状态码？
2. 哪些错误可重试？哪些绝对不能重试？为什么？
3. 指数退避的公式是什么？为什么加抖动？
4. `TaskCanceledException` 在什么情况下可重试？什么情况下不可？
5. 为什么结构化输出解析失败不应该进重试循环？
6. FinishReason 的 4 种值分别代表什么？`Length` 怎么处理？
7. 手写重试 vs Resilience Pipeline，各自的优势是什么？生产项目选哪个？
8. 「重试不是免费的」怎么理解？（Token 成本 + 延迟 + 可能请求已到达服务端）
9. CancellationTokenSource 的两层超时分别是什么？

---

# 15. troubleshooting.md

```markdown
# Day010 Troubleshooting

## 问题 1：HttpClient.Resilience 需要引用 Polly 包？
Microsoft.Extensions.Http.Resilience 已经内置 Polly 8，不需要再引
Polly 包。但 RetryStrategyOptions 属于 M.E.Http.Resilience，
不是 System.Net.Http。

---

## 问题 2：IsRetryable 里没正确区分 HttpRequestException vs ClientResultException
M.E.AI.OpenAI 抛出的异常可能被 OpenAI SDK 包装成 ClientResultException，
不是原始 HttpRequestException。检查 InnerException 或直接对基类 Exception
做 switch 匹配。

---

## 问题 3：重试循环里 Task.Delay 没传 CancellationToken
整体超时触发后，Task.Delay 会继续等待完毕再取消，浪费时间。
必须 await Task.Delay(delay, ct)。

---

## 问题 4：重试次数太多烧 Token
每次重试都重新走 API 调用链路，消耗 Token。通常 3 次足够，
超过 5 次就要考虑换更强模型 / 降 Prompt 长度 / 熔断。

---

## 问题 5：FinishReason=length + RawText 为空
推理模型 reasoning tokens 挤占 MaxOutputTokens。
修复：预算提到 4000 + closing thinking。（Day009 踩过的坑）
```

---

# 16. 面试题

## 基础

1. LLM 调用常见异常类型有哪些？各对应什么 HTTP 状态码？
2. 哪些错误可重试？哪些不能？为什么？
3. 指数退避 + 抖动的公式是什么？为什么加抖动？
4. 为什么结构化输出解析失败不应该重试？
5. FinishReason 的几种值各代表什么？

## 工程

6. 你的重试循环如何防止「无限重试 + 无限等待」？
7. TaskCanceledException 什么时候可重试，什么时候不可？
8. 重试为什么会烧 Token？一次 429 重试的 Token 成本怎么估算？
9. Microsoft.Extensions.Http.Resilience 和手写 for 循环的权衡？
10. 你怎么设计「模板渲染 → 调用模型 → 重试 → 降级」的完整链路？

## 架构

11. Phase 5 的 Circuit Breaker（熔断器）和今天的重试是什么关系？怎么组合？
12. Rate Limiter 和 Retry-After 响应头怎么配合？
13. 多租户场景下，一个租户 429 了，其他租户还要继续？你的架构怎么隔离？
14. 流式输出（Day005）的重试有什么特殊问题？（部分数据已发出，重试会重复）

---

# 17. Day010 → Day011 衔接

Day010 完成后，你的调用链路已具备：

```text
✅ 模板渲染（Day009）
✅ 可重试异常分类 + 指数退避 + 抖动（Day010）
✅ 整体超时 CancellationTokenSource（Day010）
✅ FinishReason 分类 + RawText 降级（Day006 + Day010）
✅ 不可重试异常直接抛出（Day010）
```

但还有一个**每次调用都在烧掉、却几乎没管过**的东西：**Token**。

Day011 学：

> **Token / Context Window**

```text
今天：MaxOutputTokens 是个魔法数字（500 / 4000），不知道为什么是这个数
Day011：
  ① 输入 Token 怎么数？Prompt 长度 vs Token 数（不等比）
  ② Context Window 是什么？qwen-plus 是多少？glm-5.2 是多少？
  ③ 输入 Token + 输出 Token ≤ Context Window → 超限会 400
  ④ Day009 模板化之后，Token 成本怎么算？（模板变量会膨胀）
  ⑤ Few-shot 示例占多少 Token？为什么不要加 10 个示例？
  ⑥ Token 预算策略：输入砍什么？输出砍什么？
  ⑦ Prompt Cache（Phase 5 预告）
```

到 Day011，你才能真正回答：**为什么 Day009 的 MaxOutputTokens 是 4000？qwen-plus 的 Context Window 是 128K，那为什么不能加 100 个示例？**

---

## 今日 Git Commit

```text
feat(day-010): error handling and retry lab
```
