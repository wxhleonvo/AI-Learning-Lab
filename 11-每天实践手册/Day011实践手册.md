# Day011 实践手册：Token / Context Window

> 路线位置：180 天路线 → Phase 1 → Day011
> 前置：Day001–Day010（IChatClient / ChatOptions / Structured Output / Prompt 模板化 / 错误处理）
> 今日主题：**把「MaxOutputTokens 是个魔法数字」变成「我知道为什么是这个数」——理解 Token 是什么、Context Window 怎么限制输入输出、Token 成本怎么算、怎么给 Prompt 做预算**

---

## 1. 今日目标

完成后，你必须能够：

1. 说清楚 **Token 是什么**（不是字符、不是词，是 subword 单位）以及为什么 Token 数 ≠ 字符数。
2. 区分 **输入 Token（Prompt Tokens）** 和 **输出 Token（Completion Tokens）**，知道各自计费。
3. 说出 **Context Window** 的含义，以及 qwen3.7-flash / glm-5.2 的 Context Window 大小。
4. 用 `ChatResponse.Usage` 读取本次调用的 InputTokenCount / OutputTokenCount / TotalTokenCount。
5. 用 **Tiktoken 库** 在本地预估输入 Token 数（不上 API 也能算）。
6. 设计 **Prompt Token 预算策略**：模板变量、Few-shot 示例、对话历史各占多少 Token。
7. 解释 **为什么 Day009 的 MaxOutputTokens 要设 4000**，以及推理模型的 reasoning tokens 如何挤占预算。
8. 识别 **上下文超限（400 context_length_exceeded）** 并知道怎么应对（截断 / 摘要 / 换模型）。

---

# 2. 今天只学什么

| 概念 | 今日要求 |
|---|---|
| Token | subword 单位，BPE 分词结果 |
| 输入 Token | Prompt 占的 Token，含 system / user / few-shot 示例 |
| 输出 Token | 模型生成的 Token，含 reasoning（推理模型） |
| Context Window | 输入 + 输出 ≤ 此值，超限报 400 |
| Usage 对象 | response.Usage.InputTokenCount / OutputTokenCount / TotalTokenCount |
| Tiktoken | 本地 Token 计数器，无需调 API |
| Prompt 预算 | 模板变量 + 示例 + 历史 + 系统提示 = 输入 Token 预算 |
| 截断策略 | 历史截断 / 摘要 / 换大窗口模型 |

今天**不要**学习：

- Token 缓存（Prompt Cache）——Phase 5 Day114
- Token 计费与成本核算——Phase 5 Day113（今天只算量不算钱）
- 自定义 Tokenizer / BPE 训练——Phase 6

---

# 3. 官方资料

## 3.1 .NET AI ChatResponse.Usage 文档

https://learn.microsoft.com/zh-cn/dotnet/api/microsoft.extensions.ai.usage

重点看 `UsageDetails` 类的属性：`InputTokenCount`、`OutputTokenCount`、`TotalTokenCount`、`ReasoningTokenCount`（推理模型专用）。

> 注意：10.9.0 中属性名是 `InputTokenCount` 等（早期预览版叫 `InputTokens`，已改名）。

## 3.2 OpenAI Tokenizer 在线工具（直觉体验）

https://platform.openai.com/tokenizer

输入一段中英文，看 Token 数与字符数的差异。中文一个字约 1–2 个 Token，英文一个词约 1 个 Token。

## 3.3 Tiktoken .NET 库

https://github.com/openai/tiktoken

.NET 端用 `Microsoft.DeepDev.TokenizerLib` 或 `Tiktoken` 包。今天我们用 `Tiktoken` 包。

---

# 4. 创建 Day011 项目

```powershell
cd G:\AI学习\AI-Learning-Lab
dotnet new console -n TokenWindowDemo -o Day011\TokenWindowDemo
cd Day011\TokenWindowDemo

dotnet add package Microsoft.Extensions.AI --version 10.9.0
dotnet add package Microsoft.Extensions.AI.OpenAI --version 10.9.0
dotnet add package Microsoft.Extensions.Configuration --version 10.0.11
dotnet add package Microsoft.Extensions.Configuration.Json --version 10.0.11
dotnet add package Microsoft.Extensions.Configuration.UserSecrets --version 10.0.11
dotnet add package Tiktoken --version 1.1.3
```

> 模型用 **qwen3.7-flash**（非推理模型，Token 计数清晰）。glm-5.2 的 reasoning tokens 今天单独实验。

## User Secrets

```powershell
dotnet user-secrets init
dotnet user-secrets set DashScopeKey "<你的-Key>"
dotnet user-secrets set DashScopeModel "qwen3.7-flash"
```

## csproj

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.AI" Version="10.9.0" />
    <PackageReference Include="Microsoft.Extensions.AI.OpenAI" Version="10.9.0" />
    <PackageReference Include="Microsoft.Extensions.Configuration" Version="10.0.11" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Json" Version="10.0.11" />
    <PackageReference Include="Microsoft.Extensions.Configuration.UserSecrets" Version="10.0.11" />
    <PackageReference Include="Tiktoken" Version="1.1.3" />
  </ItemGroup>
  <ItemGroup>
    <None Update="appsettings.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>
</Project>
```

---

# 5. 实验一：Token vs 字符数（直觉建立）

用 Tiktoken 本地计数，建立「字符数 ≠ Token 数」的直觉。

```csharp
using System.Text;
using Tiktoken;

Encoding.RegisterProvider(CodePagesEncodingProvider.Instance);
Console.InputEncoding = Encoding.GetEncoding(936);

// cl100k_base 是 GPT-4 / text-embedding-3 等模型用的分词器
// qwen 系列用的是自己的分词器，但 cl100k_base 作为近似估算足够
var encoder = Tiktoken.Encoding.Get("cl100k_base");

Console.WriteLine("===== 实验一：Token vs 字符数 =====");

string[] samples =
{
    "Hello, World!",
    "你好，世界！",
    "The quick brown fox jumps over the lazy dog.",
    "敏捷的棕色狐狸跳过了懒狗。",
    "LLM Token Context Window",
    "大语言模型 Token 上下文窗口",
};

foreach (var s in samples)
{
    var tokens = encoder.Encode(s);
    Console.WriteLine($"  字符数={s.Length,3}  Token数={tokens.Count,3}  比值={tokens.Count / (double)s.Length:F2}  文本：{s}");
}
```

### 预期结果（近似）

```text
字符数=13  Token数= 4  比值=0.31  文本：Hello, World!
字符数= 6  Token数= 6  比值=1.00  文本：你好，世界！
字符数=44  Token数= 9  比值=0.20  文本：The quick brown fox...
字符数=12  Token数=12  比值=1.00  文本：敏捷的棕色狐狸跳过了懒狗。
```

### 必须得出的结论

```text
英文：1 词 ≈ 1 Token（4 字符 ≈ 1 Token）
中文：1 字 ≈ 1–2 Token
中英文混排：中文 Token 密度远高于英文
所以：「输入 1000 中文字 ≈ 1000–2000 Token」，不是 1000
```

---

# 6. 实验二：ChatResponse.Usage 读取真实 Token 消耗

调一次 API，从 `response.Usage` 读取 PromptTokens / CompletionTokens / TotalTokens。

```csharp
using Microsoft.Extensions.AI;
using Microsoft.Extensions.Configuration;
using OpenAI;
using System.ClientModel;

IConfigurationRoot config = new ConfigurationBuilder()
    .AddJsonFile("appsettings.json", optional: false)
    .AddUserSecrets<Program>()
    .Build();

string? model = config["DashScopeModel"];
string? key = config["DashScopeKey"];

OpenAIClientOptions clientOptions = new()
{
    Endpoint = new Uri("https://dashscope.aliyuncs.com/compatible-mode/v1")
};

IChatClient client = new OpenAIClient(new ApiKeyCredential(key!), clientOptions)
    .GetChatClient(model).AsIChatClient();

Console.WriteLine("\n===== 实验二：ChatResponse.Usage 真实 Token 消耗 =====");

string prompt = "请用一句话解释什么是 Token。";
var response = await client.GetResponseAsync(prompt, new ChatOptions { Temperature = 0.1f, MaxOutputTokens = 200 });

Console.WriteLine($"  模型：{model}");
Console.WriteLine($"  输入 Token（PromptTokens）：{response.Usage?.InputTokenCount}");
Console.WriteLine($"  输出 Token（OutputTokens）：{response.Usage?.OutputTokenCount}");
Console.WriteLine($"  总 Token（TotalTokens）：{response.Usage?.TotalTokenCount}");
Console.WriteLine($"  推理 Token（ReasoningTokens）：{response.Usage?.ReasoningTokenCount}");
Console.WriteLine($"  回复：{response.Text}");

// 用 Tiktoken 本地估算输入 Token（与 API 返回对比）
int localTokenCount = encoder.Encode(prompt).Count;
Console.WriteLine($"\n  Tiktoken 本地估算输入 Token：{localTokenCount}");
Console.WriteLine($"  API 报告输入 Token：{response.Usage?.InputTokenCount}");
Console.WriteLine($"  差异说明：API 会加上 role 标记、特殊 token，所以比本地估算多几个");
```

### 预期结果

```text
输入 Token（PromptTokens）：15
输出 Token（OutputTokens）：12
总 Token（TotalTokens）：27
推理 Token（ReasoningTokens）：0   ← qwen3.7-flash 是 0
Tiktoken 本地估算输入 Token：12
API 报告输入 Token：15   ← 多 3 个是 role 标记 + 特殊 token
```

### 必须得出的结论

```text
Usage.InputTokenCount = 系统提示 + 用户消息 + role 标记 + 特殊 token
Usage.OutputTokenCount = 模型生成内容（含推理模型的 reasoning tokens）
Usage.ReasoningTokenCount 只在推理模型（glm-5.2 等）上有值
```

---

# 7. 实验三：Context Window 边界探测（故意超限）

qwen3.7-flash 的 Context Window 是 **128K**。构造一个 200K 字符的 Prompt，观察 400 错误。

```csharp
Console.WriteLine("\n===== 实验三：Context Window 边界探测 =====");

// 构造超长 Prompt：重复一段文本直到远超 128K Token
string longText = string.Join("\n", Enumerable.Repeat("这是一段测试文本，用于测试 Context Window 上限。", 5000));
Console.WriteLine($"  构造的 Prompt 字符数：{longText.Length}");
Console.WriteLine($"  Tiktoken 估算 Token 数：{encoder.Encode(longText).Count}");

try
{
    var r = await client.GetResponseAsync(longText, new ChatOptions { Temperature = 0.1f, MaxOutputTokens = 100 });
    Console.WriteLine($"  意外成功：{r.Text?.Substring(0, Math.Min(50, r.Text?.Length ?? 0))}");
}
catch (Exception ex)
{
    Console.WriteLine($"  异常类型：{ex.GetType().Name}");
    Console.WriteLine($"  消息：{ex.Message[..Math.Min(200, ex.Message.Length)]}");
    // 典型：context_length_exceeded / maximum context length
}
```

### 预期结果

```text
构造的 Prompt 字符数：~110000
Tiktoken 估算 Token 数：~110000+
异常：400 context_length_exceeded
消息：...maximum context length is 128000 tokens...
```

### 必须得出的结论

```text
Context Window = 输入 Token + 输出 Token ≤ 128K
超限 → HTTP 400 context_length_exceeded
应对：① 截断 Prompt  ② 摘要压缩  ③ 换更大窗口模型（glm-5.2 是 128K，部分模型 200K/1M）
```

---

# 8. 实验四：推理模型的 Reasoning Tokens 挤占预算

切到 glm-5.2，观察 `Usage.ReasoningTokenCount` 如何挤占 `MaxOutputTokens`。这是 Day009 踩的坑的量化版本。

```csharp
Console.WriteLine("\n===== 实验四：推理模型 Reasoning Tokens 挤占预算 =====");

// 临时切到 glm-5.2
IChatClient glmClient = new OpenAIClient(new ApiKeyCredential(key!), clientOptions)
    .GetChatClient("glm-5.2").AsIChatClient();

string analyzePrompt = """
    请分析下面这条评论的情感，输出 JSON：{"sentiment":"...","score":0}
    评论：这个产品续航不行，电池衰减太快了，失望。
    """;

// 故意给小预算，观察 reasoning tokens 挤占
int[] budgets = { 200, 500, 1000, 2000, 4000 };
foreach (var budget in budgets)
{
    try
    {
        var r = await glmClient.GetResponseAsync(analyzePrompt,
            new ChatOptions { Temperature = 0.1f, MaxOutputTokens = budget });
        Console.WriteLine($"  预算={budget,5}  FinishReason={r.FinishReason,-8} " +
                          $"OutputTokens={r.Usage?.OutputTokenCount,5} " +
                          $"ReasoningTokens={r.Usage?.ReasoningTokenCount,5} " +
                          $"TextLen={r.Text?.Length ?? 0,4}");
    }
    catch (Exception ex)
    {
        Console.WriteLine($"  预算={budget,5}  异常：{ex.Message[..Math.Min(80, ex.Message.Length)]}");
    }
}
```

### 预期结果（glm-5.2）

```text
预算=  200  FinishReason=Length   OutputTokens= 200  ReasoningTokens= 200  TextLen=   0  ← 全被 reasoning 吃光
预算=  500  FinishReason=Length   OutputTokens= 500  ReasoningTokens= 500  TextLen=   0
预算= 1000  FinishReason=Length   OutputTokens=1000  ReasoningTokens= 980  TextLen=  10
预算= 2000  FinishReason=Stop     OutputTokens=1200  ReasoningTokens=1100  TextLen= 100  ← 正常
预算= 4000  FinishReason=Stop     OutputTokens=1500  ReasoningTokens=1400  TextLen= 100
```

### 必须得出的结论

```text
推理模型：OutputTokens = ReasoningTokens + 正式输出 Tokens
MaxOutputTokens 预算被 reasoning 先吃掉，剩余才给正式输出
所以推理模型的 MaxOutputTokens 要设得比非推理模型大 2-5 倍
Day009 的 4000 就是这么来的（reasoning ~1400 + 输出 ~100）
```

---

# 9. 实验五：Prompt Token 预算策略（模板化场景）

把 Day009 的模板链路拿过来，拆解各部分的 Token 占比，制定预算。

```csharp
Console.WriteLine("\n===== 实验五：Prompt Token 预算拆解 =====");

string systemPrompt = "你是一位专业的客户体验分析师。请按步骤分析评论并输出 JSON。";
string fewShotExample = """
    评论：物流超快，包装完好，产品非常好用，强烈推荐！
    输出：{"sentiment":"Positive","score":9,"keywords":["物流快","包装好","产品好用"]}
    """;
string userInput = "收到货第三天了，物流确实快，包装也很结实没破损。但是续航比宣传的少很多...";

int systemTokens = encoder.Encode(systemPrompt).Count;
int exampleTokens = encoder.Encode(fewShotExample).Count;
int inputTokens = encoder.Encode(userInput).Count;

Console.WriteLine($"  系统提示 Token：{systemTokens}");
Console.WriteLine($"  1 个 Few-shot 示例 Token：{exampleTokens}");
Console.WriteLine($"  用户输入 Token：{inputTokens}");
Console.WriteLine($"  加 3 个示例后总输入 Token：{systemTokens + exampleTokens * 3 + inputTokens}");
Console.WriteLine($"  加 10 个示例后总输入 Token：{systemTokens + exampleTokens * 10 + inputTokens}");
Console.WriteLine($"\n  qwen3.7-flash Context Window = 128K");
Console.WriteLine($"  剩余给输出的预算 = 128000 - 输入 Token");
```

### 必须得出的结论

```text
Few-shot 示例是 Token 大户：1 个示例 ~50 Token，10 个就 500+
但 Few-shot 的边际收益在 3-5 个后递减（Day008 验证过）
所以：示例数控制在 3-5 个，不要无脑加 10 个
预算公式：输入 Token = system + examples * N + input + 历史
输出 Token = MaxOutputTokens
约束：输入 + 输出 ≤ Context Window
```

---

# 10. 实验六：对话历史 Token 累积与截断策略

多轮对话（Day003）中，历史消息会累积 Token。演示「滑动窗口」截断策略。

```csharp
Console.WriteLine("\n===== 实验六：对话历史 Token 累积与截断 =====");

// 模拟 10 轮对话历史
List<ChatMessage> history = new();
for (int i = 0; i < 10; i++)
{
    history.Add(new ChatMessage(ChatRole.User, $"第{i + 1}轮问题：这是一段比较长的用户输入内容，用于测试历史 Token 累积。".PadRight(100, 'x')));
    history.Add(new ChatMessage(ChatRole.Assistant, $"第{i + 1}轮回复：这是一段比较长的助手回复内容，用于测试历史 Token 累积。".PadRight(100, 'x')));
}

// 计算完整历史的 Token
string fullHistoryText = string.Join("\n", history.Select(m => $"{m.Role}: {m.Text}"));
int fullTokens = encoder.Encode(fullHistoryText).Count;
Console.WriteLine($"  10 轮完整历史 Token：{fullTokens}");

// 滑动窗口：只保留最近 4 轮（8 条消息）
int keepRounds = 4;
var trimmed = history.Skip((10 - keepRounds) * 2).ToList();
string trimmedText = string.Join("\n", trimmed.Select(m => $"{m.Role}: {m.Text}"));
int trimmedTokens = encoder.Encode(trimmedText).Count;
Console.WriteLine($"  保留最近 {keepRounds} 轮 Token：{trimmedTokens}（节省 {fullTokens - trimmedTokens}）");

// 更激进：保留最近 2 轮
var moreTrimmed = history.Skip((10 - 2) * 2).ToList();
int moreTrimmedTokens = encoder.Encode(string.Join("\n", moreTrimmed.Select(m => m.Text))).Count;
Console.WriteLine($"  保留最近 2 轮 Token：{moreTrimmedTokens}");

Console.WriteLine("\n  截断策略优先级：");
Console.WriteLine("    ① 滑动窗口（保留最近 N 轮）——最简单，丢早期上下文");
Console.WriteLine("    ② 摘要压缩（用 LLM 把早期历史总结成一段）——保信息但耗 Token");
Console.WriteLine("    ③ 混合（早期摘要 + 近期原文）——生产常用");
```

### 必须得出的结论

```text
对话历史是 Token 累积的主要来源
10 轮对话可能就 2000+ Token，长会话轻松破万
截断不是免费的：丢历史 = 模型可能忘记早期约定
生产项目：滑动窗口 + 关键信息摘要（Phase 2 Day027 会话管理会深入）
```

---

# 11. 今天不要做的事情

- 不要去算 Token 成本（钱）——Phase 5 Day113
- 不要实现 Prompt Cache——Phase 5 Day114
- 不要用 glm-5.2 做主实验模型（今天只在实验四用它看 reasoning tokens）
- 不要纠结 Tiktoken 与 qwen 分词器的精确差异（cl100k_base 估算足够）
- 不要把 Context Window 当成无限大（128K 是硬上限）

---

# 12. 今天必须完成的实验

| 实验 | 内容 | 要求 |
|---|---|---|
| 实验一 | Tiktoken 计数：中英文 Token 密度差异 | 必须 |
| 实验二 | ChatResponse.Usage 读取真实 Token | 必须 |
| 实验三 | 超长 Prompt 触发 400 context_length_exceeded | 必须 |
| 实验四 | glm-5.2 ReasoningTokens 挤占预算 | 必须 |
| 实验五 | Prompt Token 预算拆解（system + examples + input） | 必须 |
| 实验六 | 对话历史 Token 累积 + 滑动窗口截断 | 必须 |

---

# 13. notes.md 必须回答的问题

1. Token 是什么？为什么中文 Token 密度比英文高？
2. InputTokens / OutputTokens / ReasoningTokens 分别是什么？
3. Context Window 的公式是什么？超限报什么错？
4. 为什么 Day009 的 MaxOutputTokens 要设 4000？推理模型和非推理模型的差异是什么？
5. Few-shot 示例数为什么控制在 3-5 个？从 Token 角度解释。
6. 对话历史 Token 累积怎么处理？三种截断策略的权衡？
7. Tiktoken 本地估算和 API 返回的 InputTokens 为什么有差异？
8. qwen3.7-flash 和 glm-5.2 的 Context Window 分别是多少？

---

# 14. 面试题

## 基础

1. 什么是 Token？1 个中文字大约多少 Token？1 个英文词呢？
2. Context Window 是什么？输入和输出怎么共享它？
3. 推理模型的 ReasoningTokens 和 OutputTokens 是什么关系？
4. 为什么 Day009 用 glm-5.2 时 MaxOutputTokens 要设 4000？

## 工程

5. 怎么在 .NET 里读取一次调用的 Token 消耗？
6. 怎么在不上 API 的情况下本地估算 Prompt Token 数？
7. 对话历史超长了怎么处理？滑动窗口和摘要压缩各有什么取舍？
8. Prompt Token 预算怎么分配？（system / examples / history / input 各占多少）

## 架构

9. Context Window 是 128K，为什么不能把一整本书塞进去让模型总结？（提示：输出预算 + 注意力衰减 + 成本）
10. 多轮对话产品中，你怎么设计「无限历史」的体验？（提示：摘要 + 滑动窗口 + 检索）

---

# 15. Day011 → Day012 衔接

Day011 完成后，你掌握了：

```text
✅ Token 是什么 / 怎么数 / 怎么预算
✅ Context Window 限制与超限处理
✅ 推理模型 reasoning tokens 的挤占问题
✅ 对话历史截断策略
```

但还有一个问题没解决：**怎么让模型「理解」语义，而不是只靠关键词匹配？**

Day012 学：

> **Embedding 基础**

```text
今天：模型靠 Token 序列理解文本
Day012：
  ① 什么是 Embedding？（文本 → 高维向量）
  ② Embedding 怎么衡量语义相似度？（余弦相似度）
  ③ "苹果手机" 和 "iPhone" 的 Embedding 距离很近，但字符完全不同
  ④ M.E.AI 的 IEmbeddingGenerator API
  ⑤ Embedding 模型选型（text-embedding-3-small vs qwen 系列）
  ⑥ 这是 RAG（Day036+）的基础
```

到 Day012，你才能真正理解 RAG 为什么能「语义检索」而不是「关键词搜索」。

---

## 今日 Git Commit

```text
feat(day-011): token and context window lab
```
