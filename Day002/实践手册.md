# Day002｜理解 Prompt 与 Message Role —— 实践手册

> 前置条件：Day001 已能完成一次 LLM 调用。
> 今天不学习 Function Calling；辅助页面只用于查看 `ChatMessage` / `ChatRole` 写法。

## 1. 今日目标

理解：

```text
Prompt
↓
Message
↓
Role + Content
↓
LLM
```

能够独立写：

```csharp
List<ChatMessage> messages =
[
    new(ChatRole.System, "..."),
    new(ChatRole.User, "...")
];

ChatResponse response =
    await client.GetResponseAsync(messages);
```

并解释 System、User、Assistant、Message History。

## 2. 为什么重要

Day001：

```csharp
string prompt = "...";
```

今天进一步理解：

```text
System
↓
User
↓
Assistant
↓
User
↓
Assistant
```

后面的 Chat、RAG、Tool Calling、Agent 都会继续使用消息结构。

## 3. 资料

### 主资料

[Microsoft.Extensions.AI 中文文档](https://learn.microsoft.com/zh-cn/dotnet/ai/microsoft-extensions-ai)

今天重点：
- `IChatClient`
- 聊天消息
- `ChatMessage`
- AI 响应
- 消息历史相关概念

### 辅助资料

[.NET 使用 AI 模型调用函数](https://learn.microsoft.com/zh-cn/dotnet/ai/quickstarts/use-function-calling)

**只看代码中的：**
- `ChatMessage`
- `ChatRole.System`
- `ChatRole.User`

不要学习 Function Calling。

### 回顾

[.NET AI 中文文档](https://learn.microsoft.com/zh-cn/dotnet/ai/)

## 4. 创建项目

```bash
cd G:\AI学习\AI-Learning-Lab\Day002
dotnet new console -n PromptDemo
cd PromptDemo
```

目录：

```text
Day002/
├── README.md
├── 实践手册.md
├── notes.md
├── troubleshooting.md
└── PromptDemo/
```

把 Day001 已验证可用的 Provider 配置方式迁移过来。

## 5. 开始前

运行：

```bash
dotnet run
```

如果失败，先解决 Day001 的环境问题，不要带病进入新实验。

## 6. 实验一：Plain Prompt

```csharp
string prompt = """
请把下面的中文翻译成英文：

今天天气很好。
""";

ChatResponse response =
    await client.GetResponseAsync(prompt);

Console.WriteLine(response.Text);
```

运行并记录真实输出。

## 7. 实验二：System + User

```csharp
List<ChatMessage> messages =
[
    new(ChatRole.System, """
        你是一名专业的中英翻译助手。
        只输出英文翻译，不要解释。
        """),

    new(ChatRole.User, """
        今天天气很好。
        """)
];

ChatResponse response =
    await client.GetResponseAsync(messages);

Console.WriteLine(response.Text);
```

## 8. 对比实验一和二

| 实验 | 输入形式 | System | User | 实际输出 |
|---|---|---|---|---|
| 1 | String Prompt | 无 | Prompt 中包含 | 填结果 |
| 2 | ChatMessage | 有 | 有 | 填结果 |

回答：

> 两种方式都能调用模型，为什么还需要 Message Role？

先写自己的答案。

## 9. 实验三：只改变 System

User 永远保持：

```text
今天天气很好。
```

### System A

```text
你是一名专业的中英翻译助手。

只输出英文翻译，不要解释。
```

### System B

```text
你是一名英语老师。

请翻译中文，并在翻译之后解释关键语法。
```

分别运行并记录。

核心观察：

```text
User 不变
+
System 改变
↓
模型行为改变
```

## 10. 实验四：15 组 Prompt 实验

5 个输入：

```text
1. 今天天气很好。
2. 我昨天去了上海。
3. 我正在学习人工智能。
4. 这个程序运行得很慢。
5. 请帮我检查这段代码。
```

### Prompt A

```text
请翻译成英文。
```

### Prompt B

```text
你是一名专业翻译。

请把下面中文翻译成自然、准确的英文。
```

### Prompt C

```text
你是一名专业技术英语翻译。

要求：
1. 保持技术术语准确
2. 不要添加原文没有的信息
3. 只输出英文
4. 不要解释

请翻译：
...
```

5 × 3 = 15 次实验。

记录：

| 输入 | Prompt | 输出 | 长度 | 是否解释 | 术语准确性 | 我的观察 |
|---|---|---|---:|---|---|---|
| 1 | A | ... | ... | ... | ... | ... |
| 1 | B | ... | ... | ... | ... | ... |
| 1 | C | ... | ... | ... | ... | ... |

必须记录真实输出，不要编造理想结果。

## 11. 小实验：打印 Message

```csharp
foreach (var message in messages)
{
    Console.WriteLine($"{message.Role}: {message.Text}");
}
```

预期类似：

```text
System: 你是一名专业的中英翻译助手。
User: 今天天气很好。
```

这帮助你看到：

> Chat 输入是一组带角色信息的消息。

## 12. Assistant 与 Message History

今天先理解概念，不需要大量手写 Assistant。

典型结构：

```text
System
↓
User
↓
Assistant
↓
User
↓
Assistant
```

重要认知：

> 多轮聊天通常不是模型永久记住前文，而是应用保存历史消息，并在下一次请求中携带相关上下文。

## 13. notes.md 必答

1. 什么是 Prompt？
2. 什么是 System Message？
3. 什么是 User Message？
4. System 和 User 有什么区别？
5. 为什么同一句 User 输入，不同 System Prompt 会得到不同结果？
6. 多轮对话为什么需要保存 ChatMessage？
7. 为什么不能只把所有内容拼成一个大字符串？
8. `ChatRole` 提供了什么信息？
9. 为什么 Message History 对 RAG / Agent 也重要？
10. 100 轮对话是否应该全部发送给模型？

第 10 题先写自己的判断，后面 Token / Context Window 会继续回答。

## 14. 验收

- [ ] 能独立创建 `ChatMessage`
- [ ] 能使用 System / User
- [ ] 能调用 `GetResponseAsync(messages)`
- [ ] 理解 `ChatResponse.Text`
- [ ] 完成 4 类实验
- [ ] 完成至少 15 组输出
- [ ] 打印并理解 Role + Text
- [ ] 能解释 Message History
- [ ] 回答 10 个问题
- [ ] 不看教程重新写出 System + User

## 15. GitHub 产出

```text
Day002/
├── README.md
├── 实践手册.md
├── notes.md
├── troubleshooting.md
└── PromptDemo/
```

提交：

```bash
git add Day002
git commit -m "feat(day-002): prompt-message-role-lab"
```

## 16. Day001 → Day002 → Day003

```text
Day001
第一次调用模型
↓
IChatClient / ChatResponse
↓
Day002
Message / Role
↓
System / User
↓
Message History
↓
Day003
真正的多轮对话
```

Day003 不应重新教已经掌握的基础，而应从今天的 `List<ChatMessage>` 继续深入。

## 17. 如何让我生成 Day003

推荐复制：

```text
继续生成 Day003 实践手册。

请基于：
1. 180天学习路线.md
2. Day001/实践手册.md
3. Day002/实践手册.md
4. 我实际完成的 Day002/README.md
5. Day002/notes.md
6. Day002/troubleshooting.md

生成 Day003/实践手册.md。

主题：
Day003｜多轮对话与 Message History

要求：
- 中文资料优先
- 给出真实、可访问的资料链接
- 标明今天具体看什么
- 不重复已经掌握的内容
- 从 Day002 的 ChatMessage 继续深入
- 给出完整可执行步骤
- 给出 C# 代码
- 给出预期输出
- 安排实际实验
- 要求记录真实实验结果
- 给出观察和思考问题
- 给出验收标准
- 给出面试题
- 给出 GitHub 产出
- 给出 Git Commit
- 明确 Day003 与 Day004 的衔接

如果我的 Day002 记录显示某个概念没有真正掌握，
请在 Day003 开头安排 10~20 分钟补充实验。
```

## 18. 更简单的日常指令

如果上下文完整，可以说：

```text
继续 Day003。

请读取当前 180 天路线和上一天的实践记录，
按照统一实践手册标准生成 Day003。
```

如果换了新对话，最好同时提供上一天的 README / notes / troubleshooting。
